import { describe, it } from "vitest";

// TODO(owner): test allocateSchedule — spreads question_ids across days,
// respects days_available, balances minutes, handles empty input.
describe("allocateSchedule", () => {
  it.todo("allocates questions across the available days");
});

import { describe, it } from "vitest";

// TODO(owner): test validateKit — flags missing fields, bad enums, and
// dangling requirement_ids; passes on a well-formed kit.
describe("validateKit", () => {
  it.todo("validates the structure of a kit");
});

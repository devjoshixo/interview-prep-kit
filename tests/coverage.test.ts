import { describe, it } from "vitest";

// TODO(owner): test findUncovered — returns requirement ids that no question
// references; empty when every requirement is covered.
describe("findUncovered", () => {
  it.todo("reports requirements not referenced by any question");
});
